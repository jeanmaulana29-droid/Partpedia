{
  "name": "sparepart-directory",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node -e \"try{require('child_process').execSync('git fetch --deepen=200',{stdio:'ignore'})}catch(e){}\" && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "@astrojs/sitemap": "3.2.1"
  }
}
